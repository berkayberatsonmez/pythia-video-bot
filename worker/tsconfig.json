{
  "//": "Worker'a ÖZEL tsconfig (V8 isolate — Node değil). Root tsconfig worker/'ı EXCLUDE eder; bu dosya yalnız worker + paylaşılan saf src modülleri kapsar. Typecheck için: cd worker && npm install && npx tsc --noEmit",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "WebWorker"],
    "types": ["@cloudflare/workers-types"],
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*.ts"]
}
